13.doc 13.Uninstalling a Package
npm un package
example:  npm un mongoose

14.Working with Global Packages
to install a global package
(sudo) npm -g package[@version]
to Uninstall a global package
(sudo) npm un -g package

// Install a package
npm i <packageName>

// Install a specific version of a package
npm i <packageName>@<version>

// Install a package as a development dependency
npm i <packageName> —save-dev

// Uninstall a package
npm un <packageName>

// List installed packages
npm list —depth=0

// View outdated packages
npm outdated

// Update packages
npm update

- To install/uninstall packages globally, use -g flag.